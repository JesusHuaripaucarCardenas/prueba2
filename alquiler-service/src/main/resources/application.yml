server:
  port: 8082

spring:
  application:
    name: alquiler-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/jalaton}

clientes:
  service:
    url: http://localhost:8081

logging:
  level:
    org.springframework.data.mongodb.core: INFO
